<% anthology_item = Micropost.find(params[:id]) %>
<%#= form_for [anthology_item, current_user.loves.new, remote: true] do |f| %>
 
<%#= form_for(current_user.loves.build(loved_id: @micropost.id), remote: true) do |f| %>
<%= form_for(current_user.loves.build(loved_id: @micropost.id)) do |f| %>

<%= f.hidden_field :lover_id, :value => current_user.id %>
  		<%= f.hidden_field :loved_id, :value => @micropost.id %>
  <%#= f.submit "Love", class: "btn btn-mini" %>
  <%= link_to_submit 'Love' %>
<% end %>


